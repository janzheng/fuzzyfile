# Auth key required for destructive operations (delete, rename/move)
# Set this to a strong secret. Must match what's in wrangler.toml or wrangler secrets.
# For production, use `wrangler secret put DELETE_AUTH_KEY` instead of wrangler.toml vars.
DELETE_AUTH_KEY=CHANGE_ME_TO_A_REAL_SECRET
